package com.dorsetcollegeoop;

import java.util.ArrayList;
import java.util.Random;

public class Lottery {

    private ArrayList<Ticket> listOfNumbers =new ArrayList<>();

//What I was doing was really messy, gonna finish and push updated tomorrow on 11.11.2020


}


  /*private int lottery[] = new int[6];
    int random_number;

     public Lottery() {
         for (int i = 0; i < lottery.length; i++) {
             random_number = (int) (Math.random() * 50);
             for (int j = 0; j < 1; j++) {
                 if (lottery[j] == random_number){
                     random_number = (int) (Math.random() * 50);

                     j = 1;
                 }
             }
             lottery[i] = random_number;
         }

         for (int i = 0; i < lottery.length; i++) {
             System.out.println(lottery[i] + " ");
         }

     }
      code from https://stackoverflow.com/questions/28321760/java-simple-lottery-program
      just to compare the structure, but I want to finish tomorrow according to what you taught
     */
